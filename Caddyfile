{
    # Global options block
    local_certs  # Use self-signed certs for HTTPS
    admin off
    auto_https disable_redirects
}

https://77.246.103.197 {
    tls internal

    @livekit {
        path /rtc* /twirp* /livekit* /ws
    }
    reverse_proxy @livekit livekit:7880 {
        transport http {
            versions h2c 1.1
        }
    }

    @token {
        path /token*
    }
    reverse_proxy @token token-server:8000

    # Optional: Serve a static page or frontend files
    # root * /srv/www
    # file_server
}